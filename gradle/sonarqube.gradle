afterEvaluate {
  sonar {
    properties {
      property "sonar.host.url", "https://sonarcloud.io"
      property "sonar.organization", "gurkenlabs"
      property "sonar.projectKey", "de.gurkenlabs.litiengine"
      property "sonar.projectName", "LITIENGINE"
      property "sonar.projectVersion", project.version
      property "sonar.branch.name", grgit.branch.current().getName()
      property "sonar.sourceEncoding", "UTF-8"
    }
  }
}

