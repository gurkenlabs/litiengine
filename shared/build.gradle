plugins {
  id "java-library"

  id "encoding-defaults"
  id "testing-defaults"
}

dependencies {
  api libs.bundles.mockito
  api libs.junit.api
  api libs.junit.params
  runtimeOnly libs.junit.engine
}
